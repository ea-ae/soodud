# crontab -e && sudo service cron restart
0 1 * * * cd ~/soodud && docker exec soodud-django-1 /bin/bash -c 'python manage.py shell_plus -c "s.launch(); s.match()"' >> /var/log/productcron.log && docker-compose up -d --build --force-recreate >> /var/log/productcron.log
